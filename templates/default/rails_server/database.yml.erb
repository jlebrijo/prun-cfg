<%= node["environment"]%>:
  adapter: <%= (node.recipes.to_s.include? 'postgresql')? 'postgrtesql': 'mysql2' %>
  username: <%= node["db"]["user"] || 'root'%>
  password: <%= node["db"]["password"]%>
  database: <%= @db_name %>
  template: template0
